spring:
  application:
    name: back_end

  # Database
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/lensify
    username: sentify_store
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Mail
  # LƯU Ý: Nếu chưa cấu hình biến môi trường EMAIL, hãy tạm thời điền trực tiếp hoặc comment lại để tránh lỗi tiếp theo
  mail:
    host: smtp.gmail.com
    port: 587
    username: "dien_email_cua_ban_vao_day@gmail.com"
    password: "dien_mat_khau_ung_dung_vao_day"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT Configuration (Đã sửa lỗi)
security:
  jwt:
    # Điền trực tiếp chuỗi bí mật vào đây
    secret-key: "mySuperSecretKey1234567890!@#mySuperSecretKey1234567890!@#"
    # Thời gian hết hạn (tính bằng mili-giây, ví dụ 1 giờ = 3600000)
    expiration-time: 3600000

# Swagger
open:
  api:
    title: API-service document
    version: V1.0.0
    description: API for Eyewear selling
    licenseUrl: "https://example.com/license"
    licenseName: API License
    serversUrl: http://localhost:8080/
    serversDescription: Server v1

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true